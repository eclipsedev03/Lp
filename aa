-- [[ LP STORE - BLACK PREMIUM EDITION com Key System ]]
-- =====================================================
-- SISTEMA DE VALIDAÇÃO DE KEY (BLOQUEIA O MENU ATÉ VALIDAÇÃO)
-- =====================================================

local KeysValidas = {
    "LP-ABCD-1234-EFGH",
    "LP-WXYZ-5678-IJKL"
    -- Adicione mais keys aqui manualmente ou via gerador
}

local CoreGui = game:GetService("CoreGui")
local Players = game:GetService("Players")
local Player = Players.LocalPlayer
local RunService = game:GetService("RunService")

-- Verifica se já existe uma GUI de key system
local function DestroyOldKeyGui()
    local existing = CoreGui:FindFirstChild("LP_KeySystem")
    if existing then existing:Destroy() end
end

-- Cria a interface do Key System
local function CreateKeySystem()
    DestroyOldKeyGui()
    
    local ScreenGui = Instance.new("ScreenGui")
    ScreenGui.Name = "LP_KeySystem"
    ScreenGui.Parent = CoreGui
    ScreenGui.ResetOnSpawn = false
    
    local MainFrame = Instance.new("Frame")
    MainFrame.Parent = ScreenGui
    MainFrame.Size = UDim2.new(0, 450, 0, 280)
    MainFrame.Position = UDim2.new(0.5, -225, 0.5, -140)
    MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
    MainFrame.BackgroundTransparency = 0
    Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 12)
    
    local Title = Instance.new("TextLabel")
    Title.Parent = MainFrame
    Title.Size = UDim2.new(1, 0, 0, 45)
    Title.Position = UDim2.new(0, 0, 0, 0)
    Title.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
    Title.Text = "LP STORE KEY SYSTEM"
    Title.TextColor3 = Color3.fromRGB(255, 255, 255)
    Title.Font = Enum.Font.Code
    Title.TextSize = 18
    Instance.new("UICorner", Title).CornerRadius = UDim.new(0, 12)
    
    local Subtitle = Instance.new("TextLabel")
    Subtitle.Parent = MainFrame
    Subtitle.Size = UDim2.new(1, 0, 0, 25)
    Subtitle.Position = UDim2.new(0, 0, 0, 45)
    Subtitle.BackgroundTransparency = 1
    Subtitle.Text = "Insira sua chave de ativação"
    Subtitle.TextColor3 = Color3.fromRGB(150, 150, 150)
    Subtitle.Font = Enum.Font.Code
    Subtitle.TextSize = 12
    
    local KeyBox = Instance.new("TextBox")
    KeyBox.Parent = MainFrame
    KeyBox.Size = UDim2.new(0, 300, 0, 40)
    KeyBox.Position = UDim2.new(0.5, -150, 0, 80)
    KeyBox.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    KeyBox.Text = ""
    KeyBox.PlaceholderText = "LP-XXXX-XXXX-XXXX"
    KeyBox.TextColor3 = Color3.fromRGB(255, 255, 255)
    KeyBox.Font = Enum.Font.Code
    KeyBox.TextSize = 14
    Instance.new("UICorner", KeyBox).CornerRadius = UDim.new(0, 6)
    
    local ValidateBtn = Instance.new("TextButton")
    ValidateBtn.Parent = MainFrame
    ValidateBtn.Size = UDim2.new(0, 140, 0, 40)
    ValidateBtn.Position = UDim2.new(0.5, -150, 0, 140)
    ValidateBtn.BackgroundColor3 = Color3.fromRGB(40, 100, 40)
    ValidateBtn.Text = "VALIDAR KEY"
    ValidateBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    ValidateBtn.Font = Enum.Font.Code
    ValidateBtn.TextSize = 14
    Instance.new("UICorner", ValidateBtn).CornerRadius = UDim.new(0, 6)
    
    local CopyBtn = Instance.new("TextButton")
    CopyBtn.Parent = MainFrame
    CopyBtn.Size = UDim2.new(0, 140, 0, 40)
    CopyBtn.Position = UDim2.new(0.5, 10, 0, 140)
    CopyBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 80)
    CopyBtn.Text = "COPIAR KEY"
    CopyBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    CopyBtn.Font = Enum.Font.Code
    CopyBtn.TextSize = 14
    Instance.new("UICorner", CopyBtn).CornerRadius = UDim.new(0, 6)
    
    local MessageLabel = Instance.new("TextLabel")
    MessageLabel.Parent = MainFrame
    MessageLabel.Size = UDim2.new(0, 400, 0, 30)
    MessageLabel.Position = UDim2.new(0.5, -200, 0, 200)
    MessageLabel.BackgroundTransparency = 1
    MessageLabel.Text = ""
    MessageLabel.TextColor3 = Color3.fromRGB(255, 100, 100)
    MessageLabel.Font = Enum.Font.Code
    MessageLabel.TextSize = 12
    
    local function ShowMessage(text, isError)
        MessageLabel.Text = text
        MessageLabel.TextColor3 = isError and Color3.fromRGB(255, 100, 100) or Color3.fromRGB(100, 255, 100)
        task.wait(3)
        MessageLabel.Text = ""
    end
    
    local function CopyToClipboard(text)
        local clipboard = game:GetService("Clipboard")
        if clipboard then
            clipboard:set(text)
            ShowMessage("Key copiada!", false)
        else
            ShowMessage("Erro ao copiar", true)
        end
    end
    
    local function ValidateKey(inputKey)
        for _, validKey in ipairs(KeysValidas) do
            if inputKey == validKey then
                return true
            end
        end
        return false
    end
    
    ValidateBtn.MouseButton1Click:Connect(function()
        local inputKey = KeyBox.Text
        if inputKey == "" then
            ShowMessage("Digite uma key primeiro!", true)
            return
        end
        
        if ValidateKey(inputKey) then
            ShowMessage("KEY VÁLIDA! Abrindo hub...", false)
            task.wait(0.5)
            ScreenGui:Destroy()
            -- EXECUTA O SCRIPT PRINCIPAL AQUI
            LoadMainScript()
        else
            ShowMessage("KEY INVÁLIDA! Acesso negado.", true)
        end
    end)
    
    CopyBtn.MouseButton1Click:Connect(function()
        CopyToClipboard(KeyBox.Text)
    end)
end

-- Função que carrega o script principal (SEU HUB ORIGINAL)
function LoadMainScript()
    -- [[ TODAS AS FUNÇÕES DO SEU SCRIPT ORIGINAL VÃO AQUI ]]
    -- [[ INÍCIO DO SCRIPT ORIGINAL ]]
    
    local ClienteNexusLib = {}
    local TweenService = game:GetService("TweenService")
    local UserInputService = game:GetService("UserInputService")
    local RunService = game:GetService("RunService")
    local UIS = game:GetService("UserInputService")
    local Lighting = game:GetService("Lighting")
    local Camera = workspace.CurrentCamera
    local CoreGui = game:GetService("CoreGui")

    local NINJA_ICON_ID = "rbxassetid://11294863814" 

    if CoreGui:FindFirstChild("RochaGui_LightBlueBoosters") then
        CoreGui:FindFirstChild("RochaGui_LightBlueBoosters"):Destroy()
    end

    _G.CurrentHitboxScale = 5.0
    _G.HitboxEnabled = false
    _G.DirectionalJump = true
    _G.AntiAFK = false
    _G.StretchedResEnabled = false
    _G.ShowFPSCounter = false

    local ReplicatedStorage = game:GetService("ReplicatedStorage")
    local SeasonServiceRF = nil
    local ClaimSkillRemote = nil

    pcall(function()
        local Packages = ReplicatedStorage:FindFirstChild("Packages")
        if Packages then
            local Index = Packages:FindFirstChild("_Index")
            if Index then
                for _, child in ipairs(Index:GetChildren()) do
                    if child.Name:match("knit") then
                        local knit = child:FindFirstChild("knit")
                        if knit then
                            local Services = knit:FindFirstChild("Services")
                            if Services then
                                local SeasonService = Services:FindFirstChild("SeasonService")
                                if SeasonService and SeasonService:FindFirstChild("RF") then
                                    SeasonServiceRF = SeasonService.RF:FindFirstChild("RequestRankedReward")
                                end
                                
                                local SkillService = Services:FindFirstChild("SkillService") or Services:FindFirstChild("AbilityService") or Services:FindFirstChild("MasteryService")
                                if SkillService and SkillService:FindFirstChild("RF") then
                                    ClaimSkillRemote = SkillService.RF:FindFirstChild("ClaimReward") or SkillService.RF:FindFirstChild("ClaimSkill") or SkillService.RF:FindFirstChild("RequestReward")
                                end
                            end
                        end
                    end
                end
            end
        end
    end)

    local humanoid, hrp
    local function setupCharacter(char)
        if not char then return end
        humanoid = char:WaitForChild("Humanoid", 5)
        hrp = char:WaitForChild("HumanoidRootPart", 5)
        if humanoid then
            humanoid.StateChanged:Connect(function(_, state)
                if state == Enum.HumanoidStateType.Landed then humanoid.AutoRotate = true end
            end)
        end
    end
    if Player.Character then setupCharacter(Player.Character) end
    Player.CharacterAdded:Connect(setupCharacter)

    local function AddNinjaIconBehind(parent, offsetFromRight)
        local Icon = Instance.new("ImageLabel")
        Icon.Name = "NinjaIconVisual"
        Icon.Parent = parent
        Icon.BackgroundTransparency = 1
        Icon.Position = UDim2.new(1, offsetFromRight or -28, 0.5, -9)
        Icon.Size = UDim2.new(0, 18, 0, 18)
        Icon.Image = NINJA_ICON_ID
        Icon.ImageColor3 = Color3.fromRGB(255, 255, 255)
        return Icon
    end

    function ClienteNexusLib:CreateWindow(Config)
        local WindowName = Config.Name or "LP STORE"
        
        local RochaGui = Instance.new("ScreenGui")
        RochaGui.Name = "RochaGui_LightBlueBoosters"
        RochaGui.Parent = CoreGui
        RochaGui.ResetOnSpawn = false
        
        local MainFrame = Instance.new("Frame")
        MainFrame.Name = "MainFrame"
        MainFrame.Parent = RochaGui
        MainFrame.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
        MainFrame.Position = UDim2.new(0.3, 0, 0.25, 0)
        MainFrame.Size = UDim2.new(0, 560, 0, 380)
        MainFrame.ClipsDescendants = true
        Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 8)
        
        local Stroke = Instance.new("UIStroke")
        Stroke.Parent = MainFrame
        Stroke.Thickness = 2
        Stroke.Color = Color3.fromRGB(40, 40, 40)
        
        task.spawn(function()
            while task.wait(0.8) do
                pcall(function()
                    TweenService:Create(Stroke, TweenInfo.new(0.8), {Color = Color3.fromRGB(20, 20, 20)}):Play()
                    task.wait(0.8)
                    TweenService:Create(Stroke, TweenInfo.new(0.8), {Color = Color3.fromRGB(60, 60, 60)}):Play()
                end)
            end
        end)
        
        local Sidebar = Instance.new("Frame")
        Sidebar.Name = "Sidebar"
        Sidebar.Parent = MainFrame
        Sidebar.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
        Sidebar.Size = UDim2.new(0, 165, 1, 0)
        
        local Title = Instance.new("TextLabel")
        Title.Parent = Sidebar
        Title.BackgroundTransparency = 1
        Title.Position = UDim2.new(0, 12, 0, 20)
        Title.Size = UDim2.new(0, 140, 0, 25)
        Title.Font = Enum.Font.Code
        Title.Text = WindowName
        Title.TextColor3 = Color3.fromRGB(255, 255, 255)
        Title.TextSize = 16
        Title.TextXAlignment = Enum.TextXAlignment.Left
        AddNinjaIconBehind(Title, -45)
        
        local TabContainer = Instance.new("Frame", Sidebar)
        TabContainer.BackgroundTransparency = 1
        TabContainer.Position = UDim2.new(0, 10, 0, 65)
        TabContainer.Size = UDim2.new(1, -20, 1, -75)
        
        local TabButtonsLayout = Instance.new("UIListLayout", TabContainer)
        TabButtonsLayout.SortOrder = Enum.SortOrder.LayoutOrder
        TabButtonsLayout.Padding = UDim.new(0, 4)
        
        local Container = Instance.new("Frame")
        Container.Name = "Container"
        Container.Parent = MainFrame
        Container.BackgroundColor3 = Color3.fromRGB(12, 12, 12)
        Container.Position = UDim2.new(0, 175, 0, 15)
        Container.Size = UDim2.new(0, 370, 0, 350)
        Instance.new("UICorner", Container).CornerRadius = UDim.new(0, 6)
        
        local function MakeDraggable(frame)
            local dragging, dragInput, dragStart, startPos
            frame.InputBegan:Connect(function(input)
                if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                    dragging = true
                    dragStart = input.Position
                    startPos = frame.Position
                    input.Changed:Connect(function()
                        if input.UserInputState == Enum.UserInputState.End then dragging = false end
                    end)
                end
            end)
            frame.InputChanged:Connect(function(input)
                if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
                    dragInput = input
                end
            end)
            UserInputService.InputChanged:Connect(function(input)
                if input == dragInput and dragging then
                    local delta = input.Position - dragStart
                    frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
                end
            end)
        end
        MakeDraggable(MainFrame)
        
        local ToggleButton = Instance.new("TextButton")
        ToggleButton.Name = "ClienteNexusToggleButton"
        ToggleButton.Parent = RochaGui
        ToggleButton.Size = UDim2.new(0, 140, 0, 45)
        ToggleButton.Position = UDim2.new(0.05, 0, 0.2, 0)
        ToggleButton.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
        ToggleButton.Text = "LP STORE"
        ToggleButton.Font = Enum.Font.Code
        ToggleButton.TextSize = 14
        ToggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
        Instance.new("UICorner", ToggleButton).CornerRadius = UDim.new(0, 8)
        AddNinjaIconBehind(ToggleButton, -30)
        
        local TglStroke = Instance.new("UIStroke", ToggleButton)
        TglStroke.Thickness = 2
        TglStroke.Color = Color3.fromRGB(40, 40, 40)
        
        MakeDraggable(ToggleButton)
        
        task.spawn(function()
            while task.wait(0.5) do
                pcall(function()
                    TweenService:Create(ToggleButton, TweenInfo.new(0.4), {TextTransparency = 0.6, BackgroundTransparency = 0.2}):Play()
                    TweenService:Create(TglStroke, TweenInfo.new(0.4), {Color = Color3.fromRGB(20, 20, 20)}):Play()
                    task.wait(0.5)
                    TweenService:Create(ToggleButton, TweenInfo.new(0.4), {TextTransparency = 0, BackgroundTransparency = 0}):Play()
                    TweenService:Create(TglStroke, TweenInfo.new(0.4), {Color = Color3.fromRGB(60, 60, 60)}):Play()
                end)
            end
        end)
        
        ToggleButton.MouseButton1Click:Connect(function()
            MainFrame.Visible = not MainFrame.Visible
        end)

        local WindowElements = {}
        local FirstTab = true
        
        function WindowElements:CreateTab(TabName)
            local TabPage = Instance.new("ScrollingFrame")
            local TabButton = Instance.new("TextButton")
            local UICorner_Btn = Instance.new("UICorner")
            local ElementsLayout = Instance.new("UIListLayout")
            
            TabPage.Name = TabName .. "_Page"
            TabPage.Parent = Container
            TabPage.BackgroundTransparency = 1
            TabPage.Size = UDim2.new(1, 0, 1, 0)
            TabPage.ScrollBarThickness = 4
            TabPage.Visible = false
            ElementsLayout.Parent = TabPage
            ElementsLayout.SortOrder = Enum.SortOrder.LayoutOrder
            ElementsLayout.Padding = UDim.new(0, 8)
            Instance.new("UIPadding", TabPage).PaddingTop = UDim.new(0, 10)
            
            TabButton.Name = TabName .. "_Btn"
            TabButton.Parent = TabContainer
            TabButton.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
            TabButton.Size = UDim2.new(1, 0, 0, 32)
            TabButton.Font = Enum.Font.Code
            TabButton.Text = " > " .. TabName
            TabButton.TextColor3 = Color3.fromRGB(150, 150, 150)
            TabButton.TextSize = 12
            TabButton.TextXAlignment = Enum.TextXAlignment.Left
            Instance.new("UIPadding", TabButton).PaddingLeft = UDim.new(0, 8)
            UICorner_Btn.CornerRadius = UDim.new(0, 4)
            UICorner_Btn.Parent = TabButton
            AddNinjaIconBehind(TabButton, -22)
            
            if FirstTab then
                TabPage.Visible = true
                TabButton.TextColor3 = Color3.fromRGB(255, 255, 255)
                TabButton.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
                FirstTab = false
            end
            
            TabButton.MouseButton1Click:Connect(function()
                for _, page in ipairs(Container:GetChildren()) do
                    if page:IsA("ScrollingFrame") then page.Visible = false end
                end
                for _, btn in ipairs(TabContainer:GetChildren()) do
                    if btn:IsA("TextButton") then
                        btn.TextColor3 = Color3.fromRGB(150, 150, 150)
                        btn.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
                    end
                end
                TabPage.Visible = true
                TabButton.TextColor3 = Color3.fromRGB(255, 255, 255)
                TabButton.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
            end)
            
            local TabElements = {}
            
            function TabElements:CreateLabel(LabelText, CustomColor)
                local TextLabel = Instance.new("TextLabel")
                TextLabel.Parent = TabPage
                TextLabel.BackgroundTransparency = 1
                TextLabel.Size = UDim2.new(0, 350, 0, 40)
                TextLabel.Font = Enum.Font.Code
                TextLabel.Text = LabelText
                TextLabel.TextColor3 = CustomColor or Color3.fromRGB(220, 220, 220)
                TextLabel.TextSize = 13
                TextLabel.TextXAlignment = Enum.TextXAlignment.Left
                TextLabel.TextWrapped = true
                Instance.new("UIPadding", TextLabel).PaddingLeft = UDim.new(0, 12)
            end
            
            function TabElements:CreateToggle(ToggleConfig)
                local TglName = ToggleConfig.Name or "Toggle"
                local Callback = ToggleConfig.Callback or function() end
                local TglState = false
                
                local ToggleFrame = Instance.new("Frame")
                local TglTitle = Instance.new("TextLabel")
                local TglBtn = Instance.new("TextButton")
                local TglIndicator = Instance.new("Frame")
                
                ToggleFrame.Name = TglName .. "_Toggle"
                ToggleFrame.Parent = TabPage
                ToggleFrame.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
                ToggleFrame.Size = UDim2.new(0, 350, 0, 40)
                Instance.new("UICorner", ToggleFrame).CornerRadius = UDim.new(0, 4)
                
                TglTitle.Parent = ToggleFrame
                TglTitle.BackgroundTransparency = 1
                TglTitle.Position = UDim2.new(0, 12, 0, 0)
                TglTitle.Size = UDim2.new(0, 260, 1, 0)
                TglTitle.Font = Enum.Font.Code
                TglTitle.Text = TglName
                TglTitle.TextColor3 = Color3.fromRGB(220, 220, 220)
                TglTitle.TextSize = 12
                TglTitle.TextXAlignment = Enum.TextXAlignment.Left
                AddNinjaIconBehind(TglTitle, -15)
                
                TglBtn.Parent = ToggleFrame
                TglBtn.BackgroundTransparency = 1
                TglBtn.Size = UDim2.new(1, 0, 1, 0)
                TglBtn.Text = ""
                
                TglIndicator.Parent = ToggleFrame
                TglIndicator.Position = UDim2.new(0, 315, 0, 12)
                TglIndicator.Size = UDim2.new(0, 16, 0, 16)
                TglIndicator.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
                Instance.new("UICorner", TglIndicator).CornerRadius = UDim.new(0, 3)
                
                TglBtn.MouseButton1Click:Connect(function()
                    TglState = not TglState
                    local targetColor = TglState and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(40, 40, 40)
                    TweenService:Create(TglIndicator, TweenInfo.new(0.2), {BackgroundColor3 = targetColor}):Play()
                    Callback(TglState)
                end)
            end
            
            function TabElements:CreateInput(InputConfig)
                local InName = InputConfig.Name or "Input"
                local Placeholder = InputConfig.Placeholder or "Valor..."
                local Callback = InputConfig.Callback or function() end
                
                local InputFrame = Instance.new("Frame")
                local InTitle = Instance.new("TextLabel")
                local TextBox = Instance.new("TextBox")
                
                InputFrame.Name = InName .. "_Input"
                InputFrame.Parent = TabPage
                InputFrame.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
                InputFrame.Size = UDim2.new(0, 350, 0, 42)
                Instance.new("UICorner", InputFrame).CornerRadius = UDim.new(0, 4)
                
                InTitle.Parent = InputFrame
                InTitle.BackgroundTransparency = 1
                InTitle.Position = UDim2.new(0, 12, 0, 0)
                InTitle.Size = UDim2.new(0, 200, 1, 0)
                InTitle.Font = Enum.Font.Code
                InTitle.Text = InName
                InTitle.TextColor3 = Color3.fromRGB(220, 220, 220)
                InTitle.TextSize = 13
                InTitle.TextXAlignment = Enum.TextXAlignment.Left
                AddNinjaIconBehind(InTitle, -15)
                
                TextBox.Parent = InputFrame
                TextBox.Size = UDim2.new(0, 80, 0, 26)
                TextBox.Position = UDim2.new(0, 255, 0, 8)
                TextBox.BackgroundColor3 = Color3.fromRGB(28, 28, 28)
                TextBox.Font = Enum.Font.Code
                TextBox.PlaceholderText = Placeholder
                TextBox.Text = ""
                TextBox.TextColor3 = Color3.fromRGB(255, 255, 255)
                TextBox.TextSize = 13
                Instance.new("UICorner", TextBox).CornerRadius = UDim.new(0, 4)
                
                TextBox.FocusLost:Connect(function()
                    Callback(TextBox.Text)
                end)
            end

            function TabElements:CreateButton(ButtonConfig)
                local BtnName = ButtonConfig.Name or "Button"
                local Callback = ButtonConfig.Callback or function() end
                
                local MainBtn = Instance.new("TextButton")
                MainBtn.Parent = TabPage
                MainBtn.BackgroundColor3 = Color3.fromRGB(24, 24, 24)
                MainBtn.Size = UDim2.new(0, 350, 0, 38)
                MainBtn.Font = Enum.Font.Code
                MainBtn.Text = "[ " .. BtnName .. " ]"
                MainBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
                MainBtn.TextSize = 13
                Instance.new("UICorner", MainBtn).CornerRadius = UDim.new(0, 4)
                MainBtn.MouseButton1Click:Connect(Callback)
                AddNinjaIconBehind(MainBtn, -35)
            end
            
            return TabElements
        end
        
        return WindowElements
    end

    local function createOrUpdateHitboxes(scale)
        for _, model in ipairs(workspace:GetChildren()) do
            if model:IsA("Model") and model.Name:match("^CLIENT_BALL_%d+$") then
                local ball = model:FindFirstChild("Ball.001")
                if not ball then
                    local ref = nil
                    for _, p in ipairs(model:GetDescendants()) do
                        if p:IsA("BasePart") then ref = p break end
                    end
                    if ref then
                        ball = Instance.new("Part", model)
                        ball.Name = "Ball.001"
                        ball.Shape = Enum.PartType.Ball
                        ball.Size = Vector3.new(2, 2, 2) * scale
                        ball.CFrame = ref.CFrame
                        ball.Anchored = true
                        ball.CanCollide = false
                        ball.Transparency = 0.7
                        ball.Material = Enum.Material.ForceField
                        ball.Color = Color3.fromRGB(255, 255, 255)
                    end
                else
                    ball.Size = Vector3.new(2, 2, 2) * scale
                end
            end
        end
    end

    UIS.JumpRequest:Connect(function()
        if _G.DirectionalJump and humanoid and hrp then
            task.defer(function()
                task.wait(0.03)
                if not Camera then return end
                local dir = Vector3.new(Camera.CFrame.LookVector.X, 0, Camera.CFrame.LookVector.Z)
                if dir.Magnitude > 0 and hrp and humanoid then
                    hrp.CFrame = CFrame.lookAt(hrp.Position, hrp.Position + dir.Unit)
                    humanoid.AutoRotate = false
                end
            end)
        elseif humanoid then
            humanoid.AutoRotate = true
        end
    end)

    local function OptimizeGraphics()
        for _, obj in ipairs(workspace:GetDescendants()) do
            if obj:IsA("BasePart") and not obj:IsA("MeshPart") then
                obj.Material = Enum.Material.SmoothPlastic
            elseif obj:IsA("Decal") or obj:IsA("Texture") then
                obj:Destroy()
            end
        end
        Lighting.GlobalShadows = false
        Lighting.FogEnd = 9e9
        for _, effect in ipairs(Lighting:GetChildren()) do
            if effect:IsA("BloomEffect") or effect:IsA("BlurEffect") or effect:IsA("ColorCorrectionEffect") or effect:IsA("SunRaysEffect") then
                effect.Enabled = false
            end
        end
    end

    local fpsLabel = Instance.new("TextLabel")
    fpsLabel.Size = UDim2.new(0, 150, 0, 25)
    fpsLabel.Position = UDim2.new(0, 15, 0, 15)
    fpsLabel.BackgroundTransparency = 1
    fpsLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    fpsLabel.Font = Enum.Font.Code
    fpsLabel.TextSize = 14
    fpsLabel.TextXAlignment = Enum.TextXAlignment.Left
    fpsLabel.Visible = false

    local fpsValue = 0
    local lastUpdate, frameCount = os.clock(), 0
    RunService.RenderStepped:Connect(function()
        frameCount = frameCount + 1
        if os.clock() - lastUpdate >= 1 then
            fpsValue = frameCount
            if _G.ShowFPSCounter then fpsLabel.Text = "[ LP_FPS: " .. tostring(fpsValue) .. " ]" end
            frameCount, lastUpdate = 0, os.clock()
        end
        if _G.HitboxEnabled then createOrUpdateHitboxes(_G.CurrentHitboxScale) end
        if Camera then
            if _G.StretchedResEnabled then
                Camera.FieldOfView = 110
            else
                Camera.FieldOfView = 70
            end
        end
    end)

    local MinhaWindow = ClienteNexusLib:CreateWindow({
        Name = "LP STORE"
    })

    fpsLabel.Parent = CoreGui:WaitForChild("RochaGui_LightBlueBoosters")

    local AbaFarm = MinhaWindow:CreateTab("Auto Farm")
    local AbaHacks = MinhaWindow:CreateTab("Hacks")
    local AbaCharacter = MinhaWindow:CreateTab("Personagem")
    local AbaFPS = MinhaWindow:CreateTab("Segurança e FPS")
    local AbaCreditos = MinhaWindow:CreateTab("Créditos")
    local AbaComoUsar = MinhaWindow:CreateTab("Como Utilizar")

    AbaFarm:CreateToggle({
        Name = "AUTO LUCK SPIN",
        Callback = function(Value)
            _G.L = Value
            task.spawn(function()
                while _G.L do
                    if SeasonServiceRF then 
                        pcall(function()
                            while _G.L do
                                if SeasonServiceRF then 
                                    pcall(function() SeasonServiceRF:InvokeServer(1) end)
                                    print("Pegando Recompensas Lucky Spins")
                                else
                                    print("Remote Season não encontrado.")
                                end
                                task.wait(2.0)
                            end
                        end)
                    else
                        print("Remote Season não encontrado.")
                    end
                    task.wait(2.0)
                end
            end)
        end
    })

    AbaFarm:CreateToggle({
        Name = "AUTO HABILIDADE",
        Callback = function(Value)
            _G.SkillFarm = Value
            task.spawn(function()
                while _G.SkillFarm do
                    if ClaimSkillRemote then
                        for slot = 1, 5 do
                            if not _G.SkillFarm then break end
                            pcall(function() ClaimSkillRemote:InvokeServer(slot) end)
                            task.wait(0.2)
                        end
                    else
                        print("Remote de Habilidades não encontrado.")
                    end
                    print("Reivindicando Recompensas de Habilidades")
                    task.wait(2.0)
                end
            end)
        end
    })

    AbaFarm:CreateToggle({
        Name = "AUTO YEN",
        Callback = function(Value)
            _G.Y = Value
            task.spawn(function()
                while _G.Y do
                    if SeasonServiceRF then 
                        pcall(function() SeasonServiceRF:InvokeServer(2) end)
                        print("Pegando Recompensas Yen")
                    else
                        print("Remote Season não encontrado.")
                    end
                    task.wait(2.0)
                end
            end)
        end
    })

    AbaFarm:CreateToggle({
        Name = "Smart Season Rewards (All IDs)",
        Callback = function(Value)
            _G.SeasonFarm = Value
            task.spawn(function()
                while _G.SeasonFarm do
                    if SeasonServiceRF then
                        for id = 1, 100 do
                            if not _G.SeasonFarm then break end
                            pcall(function() SeasonServiceRF:InvokeServer(id) end)
                            task.wait(0.05)
                        end
                    else
                        print("Remote Season não encontrado.")
                    end
                    print("Coletando todas as recompensas da Season...")
                    task.wait(2.0)
                end
            end)
        end
    })

    AbaHacks:CreateToggle({
        Name = "Ativar Hitbox Estendida (Visual Only)",
        Callback = function(Value)
            _G.HitboxEnabled = Value
        end
    })

    AbaHacks:CreateInput({
        Name = "Tamanho da Hitbox (Multiplicador)",
        Placeholder = "Ex: 5, 10, 15",
        Callback = function(Text)
            local num = tonumber(Text)
            if num then _G.CurrentHitboxScale = num end
        end
    })

    AbaCharacter:CreateToggle({
        Name = "Pulo Direcional Inteligente",
        Callback = function(Value)
            _G.DirectionalJump = Value
        end
    })

    AbaCharacter:CreateToggle({
        Name = "Tela Estendida (Stretched FOV)",
        Callback = function(Value)
            _G.StretchedResEnabled = Value
        end
    })

    AbaFPS:CreateToggle({
        Name = "Anti AFK System (Anti Disconnect)",
        Callback = function(Value)
            _G.AntiAFK = Value
            if Value then
                pcall(function()
                    Player.Idled:Connect(function()
                        if _G.AntiAFK then
                            game:GetService("VirtualUser"):Button2Down(Vector2.new(0,0), Camera.CFrame)
                            task.wait(1)
                            game:GetService("VirtualUser"):Button2Up(Vector2.new(0,0), Camera.CFrame)
                        end
                    end)
                end)
            end
        end
    })

    AbaFPS:CreateToggle({
        Name = "Exibir Contador FPS",
        Callback = function(Value)
            _G.ShowFPSCounter = Value
            fpsLabel.Visible = Value
        end
    })

    AbaFPS:CreateButton({
        Name = "Otimizar Gráficos (Remover Texturas)",
        Callback = function()
            OptimizeGraphics()
        end
    })

    AbaCreditos:CreateLabel("Desenvolvido inteiramente por: LP STORE", Color3.fromRGB(255, 255, 255))
    AbaCreditos:CreateLabel("Versão Customizada: LP STORE Premium Edition", Color3.fromRGB(220, 220, 220))

    AbaComoUsar:CreateLabel("1. Ative os Toggles na aba 'Auto Farm' para colher recursos.", Color3.fromRGB(200, 200, 200))
    AbaComoUsar:CreateLabel("2. Use o Botão Flutuante [LP STORE] para ocultar ou exibir o menu.", Color3.fromRGB(200, 200, 200))
    AbaComoUsar:CreateLabel("3. Use a otimização de FPS em servidores pesados.", Color3.fromRGB(200, 200, 200))
    
    -- [[ FIM DO SCRIPT ORIGINAL ]]
end

-- Inicia o Key System
CreateKeySystem()

-- =====================================================
-- GERADOR DE KEYS (Script separado - execute individualmente)
-- =====================================================
--[[
local CoreGui = game:GetService("CoreGui")

local function GenerateKey()
    local function randomSegment()
        local chars = "ABCDEFGHJKLMNPQRSTUVWXYZ0123456789"
        local segment = ""
        for i = 1, 4 do
            segment = segment .. string.sub(chars, math.random(1, #chars), math.random(1, #chars))
        end
        return segment
    end
    return "LP-" .. randomSegment() .. "-" .. randomSegment() .. "-" .. randomSegment()
end

local KeyGeneratorGui = Instance.new("ScreenGui")
KeyGeneratorGui.Name = "LP_KeyGenerator"
KeyGeneratorGui.Parent = CoreGui

local MainFrame = Instance.new("Frame")
MainFrame.Parent = KeyGeneratorGui
MainFrame.Size = UDim2.new(0, 500, 0, 400)
MainFrame.Position = UDim2.new(0.5, -250, 0.5, -200)
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 12)

local Title = Instance.new("TextLabel")
Title.Parent = MainFrame
Title.Size = UDim2.new(1, 0, 0, 45)
Title.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
Title.Text = "LP STORE KEY GENERATOR"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.Font = Enum.Font.Code
Title.TextSize = 18
Instance.new("UICorner", Title).CornerRadius = UDim.new(0, 12)

local GenerateBtn = Instance.new("TextButton")
GenerateBtn.Parent = MainFrame
GenerateBtn.Size = UDim2.new(0, 200, 0, 40)
GenerateBtn.Position = UDim2.new(0.5, -100, 0, 60)
GenerateBtn.BackgroundColor3 = Color3.fromRGB(40, 100, 40)
GenerateBtn.Text = "GERAR NOVA KEY"
GenerateBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
GenerateBtn.Font = Enum.Font.Code
GenerateBtn.TextSize = 14
Instance.new("UICorner", GenerateBtn).CornerRadius = UDim.new(0, 6)

local KeyDisplay = Instance.new("TextBox")
KeyDisplay.Parent = MainFrame
KeyDisplay.Size = UDim2.new(0, 300, 0, 40)
KeyDisplay.Position = UDim2.new(0.5, -150, 0, 120)
KeyDisplay.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
KeyDisplay.Text = ""
KeyDisplay.PlaceholderText = "LP-XXXX-XXXX-XXXX"
KeyDisplay.TextColor3 = Color3.fromRGB(255, 255, 255)
KeyDisplay.Font = Enum.Font.Code
KeyDisplay.TextSize = 14
Instance.new("UICorner", KeyDisplay).CornerRadius = UDim.new(0, 6)

local CopyKeyBtn = Instance.new("TextButton")
CopyKeyBtn.Parent = MainFrame
CopyKeyBtn.Size = UDim2.new(0, 120, 0, 30)
CopyKeyBtn.Position = UDim2.new(0.5, -60, 0, 170)
CopyKeyBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 80)
CopyKeyBtn.Text = "COPIAR KEY"
CopyKeyBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CopyKeyBtn.Font = Enum.Font.Code
CopyKeyBtn.TextSize = 12
Instance.new("UICorner", CopyKeyBtn).CornerRadius = UDim.new(0, 6)

local KeysListBox = Instance.new("ScrollingFrame")
KeysListBox.Parent = MainFrame
KeysListBox.Size = UDim2.new(0, 460, 0, 150)
KeysListBox.Position = UDim2.new(0.5, -230, 0, 220)
KeysListBox.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
KeysListBox.BorderSizePixel = 0
Instance.new("UICorner", KeysListBox).CornerRadius = UDim.new(0, 6)

local KeysListLayout = Instance.new("UIListLayout")
KeysListLayout.Parent = KeysListBox
KeysListLayout.SortOrder = Enum.SortOrder.LayoutOrder
KeysListLayout.Padding = UDim.new(0, 2)

local SelectedKey = nil
local ValidKeys = {"LP-ABCD-1234-EFGH", "LP-WXYZ-5678-IJKL"}

local function UpdateKeysList()
    for _, child in ipairs(KeysListBox:GetChildren()) do
        if child:IsA("TextButton") then child:Destroy() end
    end
    
    for i, key in ipairs(ValidKeys) do
        local KeyButton = Instance.new("TextButton")
        KeyButton.Parent = KeysListBox
        KeyButton.Size = UDim2.new(1, -10, 0, 30)
        KeyButton.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
        KeyButton.Text = key
        KeyButton.TextColor3 = Color3.fromRGB(200, 200, 200)
        KeyButton.Font = Enum.Font.Code
        KeyButton.TextSize = 11
        KeyButton.TextXAlignment = Enum.TextXAlignment.Left
        Instance.new("UICorner", KeyButton).CornerRadius = UDim.new(0, 4)
        
        local RevokeBtn = Instance.new("TextButton")
        RevokeBtn.Parent = KeyButton
        RevokeBtn.Size = UDim2.new(0, 60, 1, 0)
        RevokeBtn.Position = UDim2.new(1, -65, 0, 0)
        RevokeBtn.BackgroundColor3 = Color3.fromRGB(80, 40, 40)
        RevokeBtn.Text = "REVOGAR"
        RevokeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        RevokeBtn.Font = Enum.Font.Code
        RevokeBtn.TextSize = 10
        Instance.new("UICorner", RevokeBtn).CornerRadius = UDim.new(0, 4)
        
        RevokeBtn.MouseButton1Click:Connect(function()
            for j, vkey in ipairs(ValidKeys) do
                if vkey == key then
                    table.remove(ValidKeys, j)
                    break
                end
            end
            UpdateKeysList()
        end)
        
        KeyButton.MouseButton1Click:Connect(function()
            SelectedKey = key
            KeyDisplay.Text = key
        end)
    end
end

local function CopyToClipboard(text)
    local clipboard = game:GetService("Clipboard")
    if clipboard then clipboard:set(text) end
end

GenerateBtn.MouseButton1Click:Connect(function()
    local newKey = GenerateKey()
    table.insert(ValidKeys, newKey)
    UpdateKeysList()
    KeyDisplay.Text = newKey
end)

CopyKeyBtn.MouseButton1Click:Connect(function()
    if KeyDisplay.Text ~= "" then
        CopyToClipboard(KeyDisplay.Text)
    end
end)

local CopyScriptBtn = Instance.new("TextButton")
CopyScriptBtn.Parent = MainFrame
CopyScriptBtn.Size = UDim2.new(0, 200, 0, 35)
CopyScriptBtn.Position = UDim2.new(0.5, -100, 0, 380)
CopyScriptBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 100)
CopyScriptBtn.Text = "COPIAR SCRIPT COMPLETO"
CopyScriptBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CopyScriptBtn.Font = Enum.Font.Code
CopyScriptBtn.TextSize = 12
Instance.new("UICorner", CopyScriptBtn).CornerRadius = UDim.new(0, 6)

CopyScriptBtn.MouseButton1Click:Connect(function()
    local function buildKeyListString()
        local listStr = "{"
        for i, key in ipairs(ValidKeys) do
            listStr = listStr .. '"' .. key .. '"'
            if i < #ValidKeys then listStr = listStr .. ", " end
        end
        listStr = listStr .. "}"
        return listStr
    end
    
    local fullScript = [===[
-- [[ LP STORE - BLACK PREMIUM EDITION com Key System ]]
local KeysValidas = ]===] .. buildKeyListString() .. [===[
-- (resto do script completo com LoadMainScript etc...)
-- ATENÇÃO: Cole aqui o script completo gerado automaticamente
]===]
    
    CopyToClipboard(fullScript)
end)

UpdateKeysList()
--]]